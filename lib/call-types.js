const UNARY = 'unary'
const REQUEST_STREAM = 'request_stream'
const RESPONSE_STREAM = 'response_stream'
const DUPLEX = 'duplex'

module.exports = {
    UNARY,
    REQUEST_STREAM,
    RESPONSE_STREAM,
    DUPLEX
}